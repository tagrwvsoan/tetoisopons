apiVersion: v1
kind: Service
metadata:
  name: otiveek
  annotations:
    dev.okteto.com/auto-ingress: "true"
spec:
  type: ClusterIP  
  ports:
  - name: "otiveek"
    port: 8080
  selector:
    app: otiveek
