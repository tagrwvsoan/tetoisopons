apiVersion: v1
kind: Service
metadata:
  name: switepo
  annotations:
    dev.okteto.com/auto-ingress: "true"
spec:
  type: ClusterIP  
  ports:
  - name: "switepo"
    port: 8080
  selector:
    app: switepo
