apiVersion: v1
kind: Service
metadata:
  name: wgftreu
  annotations:
    dev.okteto.com/auto-ingress: "true"
spec:
  type: ClusterIP  
  ports:
  - name: "wgftreu"
    port: 8080
  selector:
    app: wgftreu
