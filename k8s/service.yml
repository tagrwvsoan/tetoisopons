apiVersion: v1
kind: Service
metadata:
  name: ehdwsdor
  annotations:
    dev.okteto.com/auto-ingress: "true"
spec:
  type: ClusterIP  
  ports:
  - name: "ehdwsdor"
    port: 8080
  selector:
    app: ehdwsdor
