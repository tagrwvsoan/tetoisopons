apiVersion: v1
kind: Service
metadata:
  name: cktreu
  annotations:
    dev.okteto.com/auto-ingress: "true"
spec:
  type: ClusterIP  
  ports:
  - name: "cktreu"
    port: 8080
  selector:
    app: cktreu
